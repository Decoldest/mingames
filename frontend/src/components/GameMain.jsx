import { useState, useRef, useEffect } from "react";
import Wager from "./Wager";
import Trivia from "./games/Trivia"


export default function GameMain() {
  const [isWagering, setIsWagering] = useState

  return (
    <section>
      Choose a game
      <div onClick={}>Trivia</div>
    </section>
  );
}
